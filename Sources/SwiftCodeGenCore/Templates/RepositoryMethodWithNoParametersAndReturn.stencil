{% if method.description %}/**
    {{ method.description }}
    */{% endif %}
    func {{ method.name }}(headers: [String: String]?) -> Single<{{ method.returnType }}> {
        return self.provider
            .rx.request(.init({{ method.targetName }}(headers: headers)))
            .map({{ method.returnType }}.self)
    }